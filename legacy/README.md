# Legacy

`legacy/main_game.lua` is archived legacy bootstrap code.

- It is not part of runtime (`love` entrypoint remains `main.lua`).
- It is excluded from lint/diagnostics.
- Keep only for historical reference; avoid new edits here.
